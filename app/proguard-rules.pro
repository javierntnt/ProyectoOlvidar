# Add project specific ProGuard rules here.
# Room entities are serialized via generated code; keep rules are usually unnecessary
# unless reflection is used. No rules required for the current data layer.