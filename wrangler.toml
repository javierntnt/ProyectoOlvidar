# Configuración de Cloudflare Pages para el Worker de recordatorios.
# El directorio "worker" contiene _worker.js (modo avanzado de Pages),
# que se ejecuta como un Worker en el edge.
pages_build_output_dir = "worker"