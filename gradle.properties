# JVM
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX
android.useAndroidX=true

# Kotlin
kotlin.code.style=official

# AGP 9.x defaults: built-in Kotlin + new DSL are enabled by default.
# Do NOT set android.builtInKotlin=false — the opt-out is removed in AGP 10.